<h1>Quarterly Budget Details</h1>

<p id="notice"><%= notice %></p>

<p>
  <strong>Year:</strong>
  <%= @quarterly_budget.year %>
</p>

<p>
  <strong>Quarter:</strong>
  <%= @quarterly_budget.quarter %>
</p>

<p>
  <strong>Start Balance:</strong>
  <%= number_to_currency @quarterly_budget.start_balance.to_f %>
</p>

<p>
  <strong>Current Balance:</strong>
  <%= number_to_currency @quarterly_budget.current_balance.to_f %>
</p>

<p>
  <strong>End Balance:</strong>
  <%= number_to_currency @quarterly_budget.end_balance.to_f %>
</p>

<%= link_to 'Edit', edit_quarterly_budget_path(@quarterly_budget) %> |
<%= link_to 'Back', quarterly_budgets_path %>
<br>
<br>

<h2>Listing All Corresponding Expenditures</h1>
<table>

  <thead>
    <tr>
      <th>Purchase Date</th>
      <th>Cost</th>
      <th>Item</th>
      <th>Type</th>
      <th>Notes</th>
      <th>Refunded?</th>
    </tr>
  </thead>

  <tbody>
    <% @expenditures.each do |expenditure| %>
      <tr>
        <td><%= expenditure.purchase_date %></td>
        <td><%= number_to_currency(expenditure.cost) %></td>
        <td><%= expenditure.item %></td>
        <td><%= SettingsType.find(expenditure.type_id).description %></td>
        <td><%= expenditure.notes %></td>
        <td><%= expenditure.refunded.to_s.humanize %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Create New Expenditure', new_quarterly_budget_expenditure_path(@quarterly_budget.id) %>
