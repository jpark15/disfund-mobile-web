<%= form_for(@settings_type) do |f| %>
  <% if @settings_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@settings_type.errors.count, "error") %> prohibited this settings_type from being saved:</h2>

      <ul>
      <% @settings_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description, autofocus: true %>
  </div><br>
  <div class="actions">
    <%= f.submit "Update Type"%>
  </div>
<% end %>
